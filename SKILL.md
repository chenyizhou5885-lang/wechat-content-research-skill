---
name: wechat-content-research
display_name: 公众号选题情报与写作
display_name_en: WeChat Editorial Intelligence & Writing
description: 面向企业公众号的选题情报研究与成稿写作。先理解账号定位、传播任务与可用业务证据，再按目标行业动态调研原始材料、行业权威媒体、科技/创投机构、研究平台、同类内容和企业一手现场，将外部信号转化为可验证的选题判断；需要成稿时，抽象参考语料的语言特征与行文思路，执行强制去 AI 味写作，并分别交付调研报告与文章成稿。适用于选题策划、内容研究、公众号对标和文章写作；不用于只按关键词罗列文章链接。
description_zh: 从上游议题、管理问题、同类表达与企业现场中形成可验证选题并写成自然中文稿件
description_en: Research defensible WeChat editorial angles and turn them into evidence-grounded, natural Chinese drafts
category: writing
version: 0.7.0
author: 句子互动
allowed-tools: Bash,Read,Write,WebSearch,WebFetch
metadata:
  requires:
    bins:
      - node
  permissions:
    network:
      reason: 搜索公开研究报告、权威原始材料和微信公众号文章
---

# 公众号选题情报与写作

目标不是找到一批“别人写过的文章”，而是帮助企业账号形成一条可验证的选题推导链：

`上游信号 → 管理/业务问题 → 具体场景 → 企业一手证据 → 差异化判断`

## WorkBuddy 执行边界

- 公开来源发现与网页读取使用 WorkBuddy 宿主自动提供的 `WebSearch`、`WebFetch`；先定向寻找机构官网、报告原文和官方页面，不把搜索摘要当证据。
- 本 Skill 的 `scripts/` 只执行本地标准库数据清洗、门禁和已安装可选工具的状态检测，不硬编码 Token、账号、用户绝对路径或远程私有服务。
- 已知真实 `mp.weixin.qq.com` 地址时，用宿主 `WebFetch` 读取标题、日期和正文即可，不需要真实浏览器。微信文章页常有数 MB，只提取所需字段，不要整页带进上下文。
- Kimi WebBridge 是可选的本地增强，不是 WorkBuddy 内置工具，仅用于把搜狗中转链接解析成真实地址。未安装时如实记录为 `failed`，不得拿壳页或搜狗链接充当已核验。
- 公众号数据服务（新榜、红狐等）尚未作为 WorkBuddy Connector 接入，不得声称已调用。需要时按 WorkBuddy Connector 规范另行接入并由用户完成授权。不得推荐自建 RSS 抓取方案。

## 用户只问“你能做什么”时

把这类问题视为能力介绍，不要开始执行完整研究，也不要把内部流程、硬规则、门禁、检查项和系统文件名逐条展示给用户。

回答保持简短，包含三部分：

### 我能帮你做什么

- 从企业业务、账号定位和目标读者出发，寻找值得写的公众号选题。
- 调研相关行业媒体、研究机构、科技/创投内容、同类与邻近公众号，并提供可复核链接。
- 把上游观点、行业变化和企业自己的客户/业务材料连接起来，形成有依据的内容角度。
- 只做调研时交付选题情报简报；需要写作时同时交付调研报告和文章成稿。
- 根据用户样稿或本轮参考文章抽象合适的语言特征，写成自然、具体的中文稿件。

### 我不能帮你做什么

- 不编造客户案例、数字、人物原话和不存在的来源。
- 不能按账号列出某个公众号的历史文章：微信没有开放这个能力。没搜到不等于对方没写过。
- 不能保证公开搜索覆盖所有公众号文章、阅读量或平台内部数据。
- 不能把未经授权的客户隐私、内部材料或商业秘密写进公开文章。
- 不能承诺文章一定获得流量、转化或绕过 AI 检测。

### 邀请开始

用一句自然的话邀请用户开始，并告诉他最少可以提供：企业/账号名称、想写的方向，以及已有材料或链接。不要一次抛出长问卷；用户只给一个主题时，也可以先开始建立研究坐标。

除非用户主动询问“你具体怎么做”“为什么没有采用某来源”“你的时间范围是什么”或要求查看标准，否则不要主动展开内部执行规则。对外使用“调研”“核验”“准备写作”等普通说法，不使用“硬规则”“强制门禁”“通过门禁后才能写”等系统措辞。

可参考下面的表达，但根据用户语气自然改写：

> 我可以先帮你理解账号定位和这次内容要解决的问题，再调研行业媒体、研究机构、同类公众号和相关案例，整理出有来源的选题；如果你需要，我也可以继续写成文章，并同时给你一份调研报告，方便你调整观点和表达。我不会编造案例、数据或来源，也无法保证公开搜索覆盖全部公众号内容。要不要现在开始？你可以先告诉我企业/账号名称、想写的方向，以及手头有没有材料或参考链接。

## 开始前先建立研究坐标

从用户材料和对话中尽量识别以下信息；已有答案时不要重复追问：

- 账号代表谁，服务什么业务，目标读者是谁。
- 本次内容承担什么传播任务：建立思想位置、客户信任、场景教育、产品认知或转化承接。
- 想讨论的主题、行业、岗位、业务痛点和时效范围。
- 企业掌握哪些一手材料：客户访谈、交付复盘、销售/客户成功记录、产品日志、匿名数据、失败经验。
- 产品在文章中的位置，以及不能公开的客户、数据和商业信息。

信息不足但仍可研究时，明确写出暂定假设并继续；只有缺失信息会实质改变研究范围或合规边界时才询问用户。

## 构建信源组合

不要把“同类公众号”当成全部研究对象。每次研究按任务从以下层级选择信源，并说明每类信源承担的角色：

1. **企业一手现场**：形成独家答案。包括客户过程、使用数据、交付失败、业务人员观察。
2. **权威原始材料**：校准事实与概念。包括官方报告、论文、财报、监管/行业协会数据、原始访谈。
3. **上游问题源**：发现更大的变化和新概念。包括研究院、智库、大学实验室、行业研究组织。
4. **行业权威解释者**：按目标行业选择专业媒体、垂直研究平台、产业协会、科技媒体、创投媒体/机构和长期从业专家，把变化翻译为行业问题与经营判断。
5. **公共议题与综合媒体**：只有主题涉及政策、宏观经济、公共治理或社会影响时，才按需要纳入央媒、官媒和综合媒体；它们不是所有选题的默认中心。
6. **同类与邻近内容**：观察已有角度、标题语言、叙事结构、证据使用和内容空位。邻近内容可以来自相同读者、相同场景或相同问题，不要求产品同类。
7. **搜索与聚合结果**：只用于发现线索和原始出处，不直接作为事实证据。

根据企业所在行业、目标读者和选题问题动态选择具体机构。科技与创业主题可以纳入科技媒体、创投媒体、真格基金等投资机构的研究与访谈；其他行业应切换到对应的垂直媒体、协会、研究平台和头部实践者。不要把央媒、官媒、腾讯研究院、HBR、飞书或任何单一名单写成固定答案。需要规划信源时阅读 [信源地图与选题漏斗](references/research-method.md)。

开始外部检索前必须读取 [信源质量与证据准入](references/source-quality.md) 与 [定向召回与行业信源名单](references/targeted-retrieval.md)，先建立本轮优先信源池和行业名单，再执行机构优先的站内定向检索。进入名单只决定「值不值得去捞」，不决定「够不够格当证据」——名单内来源同样按 S/A/B/C 分级。链接可访问不等于证据合格；按 S/A/B/C 分级，C 级只作线索。数字、预测、公司动作、引语等核心事实必须回溯原始出处。强推荐选题至少具备 1 个 S 级原始来源、1 个独立 A 级来源和企业一手现场；不满足时降低为“待验证方向”。

同时读取 @references/source-pipeline.md，执行下面的可审计信源流水线。文字报告不能替代流水线产物。

## 可审计信源流水线

先确认本轮深度，不同深度的成本差别很大：

| 深度 | 用途 | 是否开页 |
| --- | --- | --- |
| `scan` | 选题脑暴、先看看有什么 | 不开页，只看摘要，结论全部标「待验证」 |
| `research` | 交调研报告 | 开页，但不要求公众号门禁 |
| `writing` | 要成稿 | 全部门禁生效 |

在任务输出目录执行：

1. 探测能力档位：`python3 scripts/detect_capability.py --task-dir .`。结果写入 `raw_sources.json` 的 `capability` 段。探测不到数据服务就静默按 `search_only` 继续，不要在开头盘问用户。详见 [检索能力档位与材料说明](references/source-tiers.md)。
2. 从 `templates/raw_sources.json` 复制生成 `raw_sources.json`，并把本轮 `depth` 写进去。
3. 建立本行业信源名单：用户已有 `source_registry.json` 就优先使用，没有就参照 `templates/source_registry.json` 现场生成，写入 `source_registry` 段。方法见 [定向召回与行业信源名单](references/targeted-retrieval.md)。
4. 先做名单内站内定向检索，再做动态机构检索，最后才全网兜底。至少走两种发现渠道。
5. 打开页面之前先跑预筛：`python3 scripts/prefilter_candidates.py --candidates candidates.json --out filtered.json`。门户自媒体、聚合低质站、搜狗中转链接、超窗口和无日期条目在这一步就扔掉，不要花成本打开。
6. 搜索微信公众号时执行 `node scripts/search-wechat.js "关键词" -n 10`；最终候选用 `-r` 解析真实地址。给每条结果标 `role`：支撑事实的用 `fact_source` 走全文核验，只看同行讲法的用 `landscape_only`，后者不必读全文但不能支撑任何 claim。把 `tool_attempts` 和文章字段写入 `raw_sources.json`。
7. 中途查缺口，不要等到终点：`python3 scripts/build_sources.py --raw raw_sources.json --interim`。它会告诉你还差什么、下一步最该补哪个。缺一个 S 级源时就去补 S 级源，不要继续凑候选数量。
8. 执行 `python3 scripts/build_sources.py --raw raw_sources.json --out sources.json`。只使用 `accepted_sources` 写正式台账；`rejected_sources` 写入待核验与淘汰线索。
9. 先写 `research-report.md`，开头第一节必须是「材料说明」。再执行 `python3 scripts/validate_research.py --sources sources.json --mode <scan|research|writing> --report research-report.md`。
10. 只有 `writing` 模式退出码为 0 才创建 `article-draft.md`。创建后再次执行同一命令并加 `--article article-draft.md`。

门禁退出码非零时停止写作，只交付标明缺口的研究报告与 `raw_sources.json`、`sources.json`。不得用普通 Web Search、搜索摘要或“工具当前不可用”的一句话代替公众号调研门禁。

覆盖率考核的是「名单里真读到几家」加「有几个原始出处」，不是「一共读了几篇」。名单为空时这一条自动不生效。

修改或发布 Skill 前执行 `python3 scripts/validate_skill.py` 与 `python3 -m unittest discover -s tests -v`；任一非零退出时不得打包。

## 默认时间范围

- 所有外部检索默认只召回从执行当天向前 365 天内发布的内容，包括公众号文章、研究报告、媒体文章和案例。
- 用户明确指定“近三个月”“从 2024 年开始”“不限时间”或其他范围时，以用户范围为准。
- 无法确认发布日期的结果默认不进入正式候选池；确有必要时只能作为待核验线索。
- 超过 365 天的内容默认不用于选题推荐、事实证据或语言风格抽象。
- 搜索工具不支持时间过滤时，先召回，再根据可验证的发布日期过滤；不得用旧结果补足数量。
- 当前一年内没有足够材料时，说明材料不足并缩小研究范围。只有用户同意或主动指定后，才扩大时间范围。

## 研究步骤

### 1. 把用户主题改写成问题树

至少覆盖四种问题，但不要求机械凑数：

- 上游发生了什么变化？
- 这对目标读者形成了什么经营、组织或工作矛盾？
- 现有内容通常怎样解释，遗漏了什么？
- 企业能用什么现场证据给出自己的答案？

### 2. 分层检索与回溯

- 执行前根据当前日期计算默认起始日期，并在研究坐标中写明实际检索范围。
- 搜索微信公众号文章时，优先读取 [内置搜狗微信文章搜索](references/wechat-search.md)，使用 `node scripts/search-wechat.js "关键词" -n 10` 召回一年内候选文章；对最终入选的少量候选使用 `-r`，默认通过 Kimi WebBridge 在真实浏览器中打开并核验原文。搜狗渠道失败时再降级为普通网页搜索。
- 普通网页搜索的降级结果只进入候选池和待核验线索，不能替代公众号原文门禁。声称搜狗或 WebBridge 不可用前，必须在 `tool_attempts` 保存真实命令、自动启动/重试状态和错误。
- 先搜索上游概念、变化和原始材料，再搜索管理解释和公众号表达。
- 对同一重要事实回溯原始出处；公众号转载、搜索摘要和 AI 摘要不能替代原文。
- 记录标题、作者/机构、发布日期、可点击原始链接、来源层级、核心观点、可用证据及与账号的关系。
- 外部来源没有可访问链接时，不得作为正式来源写入结论或来源台账；只能标为待核验线索，并说明缺失原因。
- 门户自媒体、聚合转载、软文/导购/横评站和主体不明页面即使链接可访问，也不得进入正式来源台账或支撑核心判断；记录到“待核验与淘汰线索”并继续追溯原始来源。
- 发现值得使用的观点时，同时记录适用条件、反例或不确定性。
- 公众号关键词搜索工具只负责“召回”，不得直接决定推荐结论。

### 3. 选题锁定后执行同类公众号调研门禁

一旦用户选择、确认或委托确定了具体选题，必须在写作前单独完成同类/竞品公众号文章调研。前面的行业扫描和零散竞品案例不能替代这一步。

先根据企业和选题建立当次竞品与内容对标池，不要只采用用户随口提到的名单：

- **直接竞品**：提供相近产品、服务或交付方式的企业。
- **头部平台与大厂产品**：占据相同用户心智或议题入口的头部账号，例如具体任务中的飞书、豆包等。
- **邻近解决方案**：服务相同读者、岗位或业务问题的产品与机构。
- **内容型对标**：不一定构成商业竞品，但长期解释相同问题、影响相同读者，例如得到大脑等内容品牌。

围绕已经确定的选题，逐个检索这些账号最近 365 天内发布的相关公众号文章。调研结果必须达到以下要求后才能进入写作：

1. 展示本次竞品/对标池及各对象入选理由。
2. 展示实际使用的检索词、账号词和时间范围。
3. 每篇文章记录标题、公众号、发布日期、可点击文章链接、与选题的关系、核心观点和表达方式。
4. 只纳入执行当天向前 365 天内的文章；用户明确指定其他范围时除外。
5. 区分“商业竞品”“用户心智竞品”“内容对标”，不能混成一个名单。
6. 总结这些文章已经说透了什么、普遍遗漏了什么、企业还能增加什么现场证据或判断。
7. 没有文章级链接、无法确认日期或只有搜索摘要的结果，不计入门禁完成数量，也不能用于语言风格抽象。内置搜狗脚本返回的中间链接必须通过 Kimi WebBridge 打开并核验最终微信 URL、标题、账号、日期和正文；只有 `verification: verified` 的结果才能计入完成数量。
8. 用户反馈搜狗召回不准或覆盖不足时，再推荐接入红狐 API 改善账号与文章检索；红狐不是默认依赖，API 返回结果仍须执行原文、日期与正文核验。

如果一年内没有找到足以支撑判断的同类文章，明确说明检索范围、失败原因和缺口。不要用旧文章补数，也不要跳过门禁直接写作；应调整检索词、扩大对标关系，或请用户决定是否放宽时间范围。

### 4. 做跨层级连接

对每个有潜力的信号完成一次转换：

1. 用一句话概括上游信号。
2. 将其翻译成目标读者真实遇到的问题。
3. 缩小到“行业 × 岗位/角色 × 场景 × 痛点”。
4. 对照同类内容，指出已被充分讨论与尚未回答的部分。
5. 匹配企业已有或需要补采的一手证据。
6. 形成企业有资格说、且能被证据支撑的文章判断。

禁止仅因为某篇文章流行、机构权威或观点新颖就推荐选题。没有企业证据时，将其标为“待验证方向”，不要包装成自家结论。

### 5. 评估选题

优先考虑以下维度：

- 读者相关性：对应真实焦虑或决策，而非泛趋势。
- 上游新意：能否带来比同类产品文章更早、更深的问题。
- 企业资格：企业是否有现场、数据、角色或方法可以回答。
- 证据强度：是否有可追溯原始材料与一手证据。
- 差异化：与已有内容相比新增了什么判断。
- 可执行性：是否能落到场景、角色、数据和流程。
- 传播适配：是否符合本次传播任务，而不是单纯追逐热点。

不要用虚假的精确总分掩盖证据缺口。可以使用“强/中/弱”并解释关键理由。

## 只做调研时的输出

默认交付一份“选题情报简报”，包含：

1. **材料说明**：放在最开头。本轮用了哪些渠道、名单里哪些账号没搜到（并说明搜不到不等于没写过）、哪些结论可以直接引用、哪些只能当线索。不要写成脚注。
2. **研究坐标**：账号、读者、传播任务、主题与关键假设。
3. **中心判断**：本轮研究最值得企业占据的思想位置。
4. **信号地图**：上游信号、管理问题、同类表达、企业现场之间的连接。
5. **推荐选题**：每个选题写明核心判断、读者问题、来源启发、企业证据、差异化、风险与下一步补采。
6. **不建议追的方向**：说明重复、证据不足、离业务太远或不具备表达资格的原因。
7. **正式来源台账**：仅列 S/A/B 级来源，提供原始链接、日期、原创性、用途、回溯状态与采用理由；事实、转述和推断分开标记。标明每条的 `role`，`landscape_only` 只能作为表达观察。
8. **待核验与淘汰线索**：列出 C 级、转载、缺日期、缺正文或无法回溯的页面及淘汰原因。
9. **素材缺口**：需要向客户、销售、交付、产品或管理层补问的问题。

`scan` 深度下只交付到第 5 项，且每条推荐都必须标「待验证」，不写正式来源台账。

需要固定字段或交给下游写作 Skill 时，读取 [输出契约](references/output-contract.md)。

## 需要写成文章时

研究与写作是两个连续阶段。先完成研究判断和材料台账，再进入写作；不得边搜边用未经核验的线索填满正文。

写作前必须读取 [自然中文写作与去 AI 味门禁](references/natural-writing.md)，并执行其中的材料门槛、风格抽象、成稿规则和交付前自检。该门禁始终生效，不以用户是否主动提出“去 AI 味”为条件。

- 用户提供可靠的个人/品牌样稿时，从样稿抽象语言特征，但不复制原句、事实、专属比喻或偶然错误。
- 用户未提供样稿时，从本轮调研中选取与账号定位、读者和文章任务匹配的多篇优质文章，只抽象共同语言特征，不模仿单一在世作者或单一账号的可识别文风。
- 研究文章只能提供表达参照，不能把其中的经历、判断和证据冒充为企业自己的材料。
- 用户明确指定的语气和格式在不违反事实、版权与本门禁的前提下优先；“模仿某人”应改为抽象高层特征。

## 写作任务的条件性交付

任务包含文章写作、改写或成稿时，先创建研究报告并通过 `validate_research.py --mode writing`。通过后分别创建：

1. `research-report.md`：调研内容、来源台账、材料选择、参考语言风格、行文思路、选题判断、证据边界和可优化项。
2. `article-draft.md`：完成自然中文与去 AI 味自检的文章成稿。

文件名可以添加主题前缀，但必须保持“调研报告”和“文章成稿”两个独立文件。保存位置优先使用用户指定目录；未指定时使用当前任务目录下清晰命名的输出文件夹。

门禁未通过时不得创建 `article-draft.md`，只交付研究报告、`raw_sources.json`、`sources.json` 和明确补采动作；这不算写作任务完成。

调研报告是给用户审阅和二次优化的编辑工作底稿，不是内部思考过程或模型推理记录。必须明确展示：

- 开头的「材料说明」：本轮渠道、没搜到的名单账号、可直接引用的结论、只能当线索的部分。
- 为什么选择这些信源，以及它们在当前行业中的权威性来自哪里。
- 确定选题后调研了哪些商业竞品、用户心智竞品和内容对标账号，以及对应的文章级链接。
- 使用了哪些材料、舍弃了哪些方向、还有哪些证据缺口。
- 从哪些文章抽象了哪些语言特征；只写可观察特征，不复制原句或模仿单一作者。
- 成稿采用什么开头、结构、叙事顺序、证据顺序、产品位置和结尾方式。
- 用户可以在哪些位置替换判断、补充案例、调整语气或改变文章方向。

文章成稿只保留读者需要看到的内容，不混入风格分析、材料台账和写作说明。

## 质量门槛

- 推荐选题必须能追溯到信号和证据，不能只凭模型常识。
- 强推荐选题必须满足 `S 级原始来源 + 独立 A 级来源 + 企业一手现场`；否则降低推荐级别。
- 大门户域名不等于权威来源，必须核对实际发布者、作者、原创性与引用链。
- 至少同时使用上游/原始来源与同类/邻近内容；除非用户明确限制研究范围。
- 搜索结果是线索，不是证据；重要数字、引语和结论必须回溯。
- 清楚区分外部观点、企业已验证事实、研究者推断和待验证假设。
- 产品应作为实现路径或现场证据出现，不自动成为文章主角。
- 不伪造阅读量、影响力、客户数据或无法获得的平台指标。
- 涉及客户案例时先做隐私和授权判断；未获授权的材料只用于内部研究或匿名表达。
